# REQ-CADDY-S02-01 / REQ-CADDY-S02-02 — canary-weighted HTTPRoute.
#
# Argo Rollouts Gateway API plugin mutates backendRef weights during a
# caddy-origin canary (100/0 → 20 → 50 → 100). Committed values are the STABLE
# steady state. `workloads` Application ignoreDifferences covers this route.
#
# REQ-CADDY-S05-03 — dual attachment (mulligan vs mulligan-bg pattern):
#   caddy-mvp          → origin-stable/canary (canary AnalysisTemplate story)
#   caddy-mvp-showcase → nginx-proxy-active → ConfigMap proxy_pass → caddy-origin
#
# Showcase match uses header X-Kaddy-Showcase: nginx (plus PathPrefix /) so the
# full content paths (/docs/, /slides/) reach Caddy unchanged — a PathPrefix
# /via-nginx hop would need an nginx rewrite we do not own in this lane. The
# Gateway API plugin still only mutates HTTPRoute caddy-mvp.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: caddy-mvp
  namespace: caddy-mvp
  labels:
    owner: platform-team
    service: caddy-mvp
    part-of: kaddy
    managed-by: argocd
    data-classification: public
    business-criticality: business-operational
    track: stable
    app.kubernetes.io/name: caddy-mvp
    app.kubernetes.io/part-of: kaddy
    app.kubernetes.io/managed-by: argocd
  annotations:
    kaddy.io/req: REQ-CADDY-S02-01
spec:
  parentRefs:
    - name: caddy-mvp
      namespace: caddy-mvp
      group: gateway.networking.k8s.io
      kind: Gateway
      sectionName: https
  hostnames:
    - caddy-mvp.kaddy.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: caddy-origin-stable
          port: 8080
          weight: 100
        - name: caddy-origin-canary
          port: 8080
          weight: 0
---
# REQ-CADDY-S05-03 — showcase topology hop: Gateway → nginx → Caddy origin.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: caddy-mvp-showcase
  namespace: caddy-mvp
  labels:
    owner: platform-team
    service: caddy-mvp
    part-of: kaddy
    managed-by: argocd
    data-classification: public
    business-criticality: business-operational
    track: stable
    app.kubernetes.io/name: caddy-mvp-showcase
    app.kubernetes.io/part-of: kaddy
    app.kubernetes.io/managed-by: argocd
  annotations:
    kaddy.io/req: REQ-CADDY-S05-03
spec:
  parentRefs:
    - name: caddy-mvp
      namespace: caddy-mvp
      group: gateway.networking.k8s.io
      kind: Gateway
      sectionName: https
  hostnames:
    - caddy-mvp.kaddy.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
          headers:
            - name: X-Kaddy-Showcase
              value: nginx
      backendRefs:
        - name: nginx-proxy-active
          port: 8080
