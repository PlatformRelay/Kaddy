# workloads (app-of-apps child target)

Tracked-but-empty directory that the `workloads` child Application
(`deploy/apps/workloads.yaml`) syncs. Home for the demoable sample apps (Caddy /
nginx MVP variants, clubhouse) once scaffolded. Argo CD tolerates a directory
with no renderable manifests, so this README keeps the path present in Git.
