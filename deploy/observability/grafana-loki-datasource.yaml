# REQ-E3-S02-05: Loki wired as a Grafana datasource (the DEFAULT logs source).
#
# The kube-prometheus-stack Grafana sidecar (sidecar.datasources, label
# grafana_datasource=1) watches for ConfigMaps like this one and provisions the
# datasource into Grafana at runtime. Loki is the SINGLE logs datasource, so it
# is the de-facto logs source for Explore. It is intentionally NOT marked
# isDefault: Grafana allows exactly one default datasource per org, and the
# kube-prometheus-stack chart already registers Prometheus as that default; a
# second default makes Grafana refuse to start ("Only one datasource per
# organization can be marked as default"). Matches the spec verify (type: loki).
#
# NOTE: this ConfigMap declares its own namespace (monitoring) so the Argo CD
# `observability` directory-app applies it there, not into the app's default ns.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-loki-datasource
  namespace: monitoring
  labels:
    # Sidecar discovery label — MUST match the chart's sidecar.datasources.label.
    grafana_datasource: "1"
    owner: platform-team
    service: observability
    part-of: kaddy
    managed-by: argocd
    data-classification: internal
    business-criticality: business-operational
    track: stable
    app.kubernetes.io/name: grafana-loki-datasource
    app.kubernetes.io/part-of: kaddy
    app.kubernetes.io/managed-by: argocd
data:
  loki-datasource.yaml: |-
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        access: proxy
        # In-cluster Loki single-binary service (port 3100).
        url: http://loki.monitoring.svc.cluster.local:3100
        jsonData:
          maxLines: 1000
        editable: false
