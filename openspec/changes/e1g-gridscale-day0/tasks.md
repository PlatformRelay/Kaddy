# Tasks — E1g gridscale day-0

Legend: `[x]` offline-authored + proven by `task test:smoke:e1g`; live apply is a
later serialized step (`task e1g:up`). See docs/runbooks/gridscale-day0.md.

- [x] E1g-S01: Terramate root + gridscale provider (`~> 2.2`) + object-storage backend anchor (offline: codegen + validate + tofu test)
- [x] E1g-S02: Network + firewall + IPs + conftest (offline)
- [x] E1g-S03: GSK cluster + one minimal node pool (offline; conftest caps release/sizing) — **LIVE-PROVEN 2026-07-17**: `kaddy-gsk` (1 node, release 1.30) provisioned on gridscale, `kubectl get nodes` → `node-pool-0-0 Ready v1.30.14`, torn down, tenant audited clean. Evidence: `evidence/live/e1g-gsk-2026-07-17.md`.
- [x] E1g-S04: LBaaS entry point (offline)
- [x] E1b-S04 (descoped here): Terramate codegen injects `modules/labels` into every stack
- [~] E1g-S05: kubeconfig + ArgoCD re-bootstrap + Dex public issuer URL — kubeconfig retrieval **LIVE-PROVEN** (see S03); app-of-apps re-sync + edge/TLS swap **splits into S05a–S05g (cloud-edge substrate swap)** — full story bodies in `agent-context/BACKLOG.md` § "Phase-2 gridscale cloud-edge". Confirmed UNBUILT 2026-07-18 while attempting a live standing demo: `task e8b:up` is guard-locked to `kind-kaddy-dev`, and GSK has no ingress edge (Gateway API/GatewayClass/LB-IPAM are kind-only via E1e).
  - [ ] Live conftest carve-out: exclude `gridscale_object_storage_*` from the live-plan `labels.rego` run — the provider gives those resources no `labels` arg (see runbook "Live conftest carve-out"). Do NOT relax `labels.rego`.
  - [x] E1g-S05a: GSK-targetable bootstrap opt-in — named GSK context (`KADDY_GSK_CONTEXT`) past the kind-only guard via shared `hack/lib/guard-context.sh` (all 7 `bootstrap:*` tasks); offline six-branch guard test `tests/smoke/bootstrap-guard.sh` wired into `test:smoke:e1g`; documented in `docs/runbooks/gridscale-live-demo.md`. Independent review APPROVE (no P0/P1) 2026-07-18.
  - [~] E1g-S05d: **COLLAPSED** — GSK has a working service-LoadBalancer CCM (`type=LoadBalancer` auto-provisions a gridscale loadbalancer + public IP, proven `185.241.34.187`), so no manual LBaaS↔network topology reconciliation is needed for the edge. GSK still mints its own `k8s_private_network_uuid`; the standalone network stack stays offline IaC (no dep on the edge).
  - [x] E1g-S05b: **LIVE-PROVEN 2026-07-18** — Gateway API v1.5.1 CRDs (isIP/isCIDR/isURL CEL rules stripped for GSK's k8s 1.30) + **Traefik** Gateway API controller (D-042: GSK's managed Cilium CANNOT serve Gateway API). Codified: `deploy/gateway-controller/traefik/` (ArgoCD app + values; chart owns the `traefik` GatewayClass) + `hack/gsk/apply-gatewayapi-crds.sh`. Evidence: `evidence/live/e1g-cloud-edge-live-2026-07-18.md`.
  - [~] E1g-S05c: **COLLAPSED** — subsumed by the LoadBalancer CCM (see S05d). The Traefik `type=LoadBalancer` Service maps external 443→8443 (entrypoint `websecure:8443`); no NodePort + manual LBaaS forwarding needed. **Security constraint (E1g-S05h):** GSK worker-node public IPs stay internet-reachable on ALL NodePorts even behind the LB CCM (the CCM LB backs onto a NodePort) — route north-south only through the LB; add default-deny NetworkPolicy/HostFirewall. See `docs/security/e1g-s05h-gsk-node-public-ip-2026-07-18.md`.
  - [x] E1g-S05e: **LIVE-PROVEN 2026-07-18** — real `*.lab.platformrelay.dev` hostnames via cloud-only overlay (`deploy/gateway/cloud-only/`: `clubhouse` Gateway + 3 HTTPS listeners :8443 + per-host Certificates + argocd/grafana/caddy-demo HTTPRoutes). Kind stays `.kaddy.local` (excluded-by-location, recurse:false).
  - [x] E1g-S05f: **LIVE-PROVEN 2026-07-18** — DNS-01 Cloudflare ClusterIssuers (staging + prod) + token-LESS ExternalSecret/SecretStore in `deploy/cert-manager/cloud-only/` (cloud-only; kind stays HTTP-01). Cloudflare API token NEVER committed — lives in Secret `cloudflare-api-token` (ns cert-manager).
  - [x] E1g-S05g: **LIVE-PROVEN 2026-07-18** — Cloudflare A records `{argocd,grafana,demo}.lab.platformrelay.dev` → the Traefik LB IP (proxied=false) + LE prod certs issued + public serve verified (`https://argocd.lab.platformrelay.dev` → HTTP 200, chain verifies without `-k`). Evidence: `evidence/live/e1g-cloud-edge-live-2026-07-18.md`.
  - [x] E1g-S05h: **Security spike DONE 2026-07-18** — GSK worker-node public-IP exposure is an **accepted risk with no IaC/API mitigation**: `gridscale_k8s` (provider 2.3.0) exposes no disable-public-IP / node-firewall arg (`security_zone_uuid` deprecated; `k8s_private_network_uuid` read-only). Compensating controls: LBaaS-only ingress, default-deny NetworkPolicy, minimal node count, ruthless teardown. No plan-time conftest guard (runtime `type=NodePort/LoadBalancer` is invisible to a tofu plan). Independent review APPROVE. Doc: `docs/security/e1g-s05h-gsk-node-public-ip-2026-07-18.md`.
  - [x] E1g-S05i: **LIVE-PROVEN 2026-07-18** — the FULL **caddy-mvp** Argo Rollouts canary on the cloud-edge (`https://caddy.lab.platformrelay.dev` → HTTP 200, LE prod cert verifies without `-k`, showcase image `ghcr.io/platformrelay/kaddy-showcase:0.1.1`). Codified cloud-only: `https-caddy` listener + `caddy-tls` Certificate + caddy-mvp HTTPRoute (backendRefs `caddy-origin-stable` 100 / `caddy-origin-canary` 0) in `deploy/gateway/cloud-only/`. **Arch caveat (also closes E7 on GSK):** `deploy/rollouts/config.yaml` pins the gatewayAPI plugin `...-linux-arm64` (kind/Apple-Silicon); GSK nodes are amd64 → arm64 binary `exec format error` stalls ALL rollout reconciliation. `hack/gsk/rollouts-plugin-amd64.sh` live-patches the ConfigMap to `...-linux-amd64` (same v0.16.0) + restarts the controller; kind arm64 default untouched. Independent review APPROVE.
  - Build order (as built, LIVE-PROVEN 2026-07-18): **S05a → S05b → (S05e ∥ S05f) → S05g → S05i.** S05c/S05d COLLAPSED (GSK LoadBalancer CCM removed the manual NodePort/LBaaS wiring). S05h independent security spike DONE.
  - [x] E1g-S06: Reconcile the retired "no standing env" DECIDED-B prose → go-live recorded+time-boxed carve-out (ROADMAP/both gridscale runbooks/Taskfile e8b desc+comment strings; INBOX left to the edge session) + offline doc-truth guard `tests/meta/e1g-standing-policy.yaml` wired into `test:meta:ci`; forward-refs S07 + D-042 + `evidence/live/e1g-gsk-2026-07-18.md`. Doc-truth/cost-governance, NOT a blocker. Closes audit WIP-D1 (doc half). Body in `agent-context/BACKLOG.md` § "Go-live cost governance".
  - [ ] E1g-S07: Cost-visibility standing marker + `task e1g:status` + SOFT time-box WARN guardrail (~14d default, always exit 0, absent=no-op) wired into `test:meta:ci`; `task verify` EXIT 0 even with a stale marker (softness proof). Closes audit WIP-D1 (TTL half). Depends S06.
- [x] Gate: `task test:smoke:e1g` (offline) — EXIT 0
- [ ] Live-proof: `task e1g:up` then re-sync app-of-apps (later serialized step, costs money)
