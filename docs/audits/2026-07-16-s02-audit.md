# kaddy audit — 2026-07-16 (E11-S02)

Agent-assisted security & compliance audit (E11-S02). Second dated run of the
committed procedure in [README.md](README.md). Cluster evidence collected
**read-only** from kind context `kind-kaddy-dev` at repo commit `8091649`.

## Summary

- Overall: **PASS WITH NOTES**
- Compared to: [2026-07-16-audit.md](2026-07-16-audit.md) (E11-S01)
- Findings: **0 × P0 · 1 × P1 · 7 × P2 · 2 × P3** (counts active at this run)
- Blocking: 1 (F-01 — still open in cluster)

### Tooling used / skipped

| Tool | Status | Note |
| --- | --- | --- |
| kubectl (get/list only) | used | netpols, Argo apps, workloads, namespaces |
| gitleaks | skipped this run | S01 clean; no secret-touching diff since |
| kyverno CLI | skipped this run | S01 28/28; no policy test tree change this session |
| Trivy / cosign | CI-only | unchanged gates on `main` |

## Diff vs previous (E11-S01)

### Fixed (git / platform — not yet fully reflected in cluster)

| ID | Note |
| --- | --- |
| F-01 (repo) | `deploy/policies/network/mulligan.yaml` **now exists on `main`** (was absent at S01). Cluster still has **zero** NetworkPolicies in `mulligan` because Argo CD Application `policies` is **OutOfSync**. |
| — | e-caddy-mvp Variant B manifests + re-homed monitoring Application landed on `main` (`caddy-mvp-monitoring`). |

### New

| ID | Sev | Area | Finding | Location | Blocks? |
| --- | --- | --- | --- | --- | --- |
| F-12 | P2 | A1 / GitOps | Namespace `caddy-mvp` is live (Gateway, nginx-proxy Rollout, Services) but has **no NetworkPolicies** in-cluster. Repo has `deploy/policies/network/caddy-mvp.yaml`; `policies` Application remains OutOfSync so the default-deny never applied. | cluster ns `caddy-mvp`; App `policies` | No |
| F-13 | P2 | A1 / GitOps | Child Application `caddy-mvp-monitoring` is on `main` but **not present** in the cluster (`applications.argoproj.io not found`). Root is Synced — likely needs a root refresh/sync wave to discover the new sibling under `deploy/apps/`. Until then the re-homed PodMonitor/PrometheusRules stay unapplied. | `deploy/apps/caddy-mvp-monitoring.yaml` | No |
| F-14 | P2 | A1 / GitOps | `workloads` Application is **OutOfSync** while `caddy-mvp` tenant objects partially exist (nginx-proxy up; origin Rollout pods not listed in this snapshot). Drift risk for showcase/canary story. | App `workloads` | No |

### Regressed

- None vs S01 severity ladder. F-01 remains the sole P1 **in cluster** (git gap closed; sync gap remains).

### Still open (unchanged from S01)

| ID | Sev | Status |
| --- | --- | --- |
| F-01 | P1 | **Open in cluster** — mulligan netpol manifests in git, not applied (`policies` OutOfSync) |
| F-02–F-09 | P2 | Assumed still relevant; not all re-validated this run (focus = diff + GitOps sync posture) |
| F-10–F-11 | P3 | Not re-checked |

## Findings (this run — material items)

| ID | Sev | Area | Finding | Location | Blocks? |
| --- | --- | --- | --- | --- | --- |
| F-01 | P1 | A1 Network | `mulligan` still has **no NetworkPolicy** objects while serving public-facing HTTPRoute traffic. Repo now has `deploy/policies/network/mulligan.yaml`; apply is blocked by Argo `policies` **OutOfSync**. | cluster ns `mulligan`; App `policies` | Yes |
| F-12 | P2 | A1 Network | `caddy-mvp` live without netpols (see Diff). | cluster ns `caddy-mvp` | No |
| F-13 | P2 | GitOps | `caddy-mvp-monitoring` Application missing in-cluster. | argocd | No |
| F-14 | P2 | GitOps | `workloads` OutOfSync with partial tenant objects. | argocd | No |

## Compliance mapping

Asset inventory / segmentation expectations (NIS2-style): public-facing demo namespaces
(`mulligan`, `caddy-mvp`) must carry default-deny NetworkPolicies. Git now holds the
policies; **GitOps sync debt** is the active control gap — same class as S01 F-01, with
a clearer root cause (OutOfSync `policies` app).

## Operator follow-ups (not part of this audit lane)

1. Sync / reconcile Argo Applications `policies` and `workloads` (and refresh `root` so
   `caddy-mvp-monitoring` is discovered).
2. Re-run netpol assertions: `kubectl get netpol -n mulligan -n caddy-mvp`.
3. Un-skip caddy-mvp Chainsaw suites once sync is Healthy.

## History row

See [README.md](README.md) History table (updated this lane).
