# E1g live proof — GSK substrate STANDING on gridscale (2026-07-18)

Operator-authorized **standing** bring-up (NOT the usual ephemeral create→verify→destroy —
the operator opted into leaving it up to attempt a live public demo; see the session handover).
Substrate = object-storage state anchor → network (public IPv4 `185.241.34.52`) → GSK cluster.
The full public edge (Gateway/TLS/DNS) was found UNBUILT and deferred to stories E1g-S05a–h.

## Standing substrate (all billing until `task e1g:down`)

| Stack | Resource | ID / value |
| --- | --- | --- |
| object-storage | bucket (S3 state anchor) | `gos3.io/kaddy-tfstate` |
| network | public IPv4 (LBaaS listen / DNS target) | `185.241.34.52` |
| k8s (GSK) | cluster UUID | `e2ac442d-7026-4577-8f24-086cfea61be5` |
| k8s (GSK) | GSK-managed private network UUID | `e96c90cd-8345-46e8-abb2-1c245e2ac7ae` |

## `kubectl get nodes -o wide` against the exported kubeconfig

```text
NAME            STATUS   ROLES    AGE     VERSION    INTERNAL-IP   EXTERNAL-IP      OS-IMAGE             KERNEL-VERSION       CONTAINER-RUNTIME
node-pool-0-0   Ready    <none>   3m25s   v1.30.14   10.244.0.2    185.241.34.168   Ubuntu 22.04.5 LTS   5.15.0-171-generic   containerd://1.6.33
```

Context: `kaddy-gsk-admin@kaddy-gsk`. Server version: `v1.30.14`.

## Proves / does NOT prove

- **Proves:** `gridscale_k8s` provisions a reachable GSK cluster (release 1.30, 9m53s create);
  kubeconfig output works; the object-storage + network stacks provision and thread their outputs.
- **Does NOT prove:** any public ingress / TLS / app-of-apps sync — `task e8b:up` is guard-locked to
  `kind-kaddy-dev` and GSK has no ingress edge out of the box (Gateway API/GatewayClass/LB-IPAM are
  installed only by the kind E1e bring-up). This is the deferred cloud-edge work (E1g-S05a–g).
- **Security note (→ E1g-S05h):** the GSK worker node came up with a **public** EXTERNAL-IP
  (`185.241.34.168`), and the platform network stack's firewall is orphaned (nodes attach to GSK's own
  `k8s_private_network_uuid`, not `gridscale_network.platform`). `gridscale_k8s` (`~> 2.2`) exposes no
  argument to disable node public IPs — flagged for investigation.

## Teardown (stop the meter)

```bash
task e1g:down   # destroys lbaas → k8s → network → object-storage; verify the panel is clean
```
