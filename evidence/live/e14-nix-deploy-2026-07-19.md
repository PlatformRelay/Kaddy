# Evidence — E14-S03 Nix VM deploy: mechanism proven, boot-to-serve NOT yet (ADR-0303) — 2026-07-19

**Story:** E14-S03 · **Change:** `openspec/changes/e14-nix-golden-images/` · **Status:** PARTIAL (honest)

## What IS proven

The **deploy mechanism** works for the Nix Marketplace app exactly as for Caddy (E13-S05): a
`gridscale_server` with a `gridscale_storage` created from `template.template_uuid =
3aa9777e-…` (the `kaddy-nix` consumer import) + a public IPv4 + the gridscale Public Network
**provisions and powers on** (`power=true`, `status=active`, Public Network + IP `185.241.34.52`
attached — byte-identical wiring to the Caddy VM that serves in ~20 s).

## What is NOT yet proven — the from-scratch boot contract (ADR-0303's flagged risk)

The Nix VM **does not serve** over `:80`/`:2019` within a 6-minute window — across **two** live
attempts, each provisioned + powered on cleanly, each torn down clean (0 orphans):

| Attempt | Image | Result |
| --- | --- | --- |
| v1 | `raw` (nixos-generators), no qemu-guest profile | `GET /` = 000 for 6 min; VM active |
| v2 | `raw` + **`profiles/qemu-guest.nix`** (virtio_blk/virtio_net in initrd) | `GET /` = 000 for 6 min; VM active |

The Caddy (Packer/Ubuntu) image serves in ~20 s over the *same* networking, so the edge/routing is
proven — the gap is **inside the from-scratch NixOS image booting on gridscale**.

## Diagnosis — narrowed by a local QEMU boot test (the discriminator)

gridscale's console is VNC (not API-readable), so to get boot-stage signal the fixed `.gz` was
**booted locally in QEMU (TCG) with serial capture** — `qemu-system-x86_64 -drive
file=nixos.img,if=virtio -device virtio-net-pci -serial file:… -netdev user,hostfwd=tcp::8080-:80`.
Result — the serial console shows a **full, successful boot**:

```console
SeaBIOS … Booting from Hard Disk …
GRUB loading. Welcome to GRUB!  Booting `NixOS'
<<< Welcome to NixOS 24.11.20250630.50ab793 (x86_64) - ttyS0 >>>
nixos login:
```

…yet `GET localhost:8080/` (hostfwd → guest :80) returned `000` throughout — **the image boots but
Caddy does not serve**, the *same* symptom as on gridscale. This is decisive:

1. **Firmware mismatch — RULED OUT.** gridscale default = i440fx/BIOS; the image is BIOS/MBR (`raw`). It
   also boots under QEMU SeaBIOS. `raw-efi` is *not* the fix.
2. **Image broken / doesn't boot — RULED OUT.** It reaches `nixos login:` locally (kernel + initrd +
   virtio + systemd + getty all fine — the qemu-guest/virtio fix works).
3. **`.gz` format / gridscale import mismatch — RULED OUT.** The *same* raw disk boots identically in
   local QEMU; a gridscale-specific import problem would not reproduce locally. (This was the prior
   leading hypothesis — the QEMU test refuted it, saving a wasted snapshot-export rebuild.)
4. **✅ ACTUAL CAUSE (narrowed): the image boots but Caddy/network doesn't serve — a NixOS *config* bug,
   locally reproducible.** The identical Caddyfile serves fine on the Ubuntu/Packer VM (E13-S05), so the
   gap is NixOS-specific: candidates are (a) `services.caddy` under `admin off` not coming up / not
   binding :80 (Type=notify readiness, cap_net_bind), (b) the `/srv` `tmpfiles` symlink not ready when
   Caddy starts, or (c) `networking.useDHCP` not bringing the NIC up (no IP → unreachable on both QEMU
   hostfwd and the gridscale public IP). **Fix path (local, fast, no gridscale cost):** rebuild a
   *debuggable* image (root password + `systemctl status caddy` / `ip -br a` / `journalctl -u caddy` to
   ttyS0), boot in QEMU, read the exact failure, fix the module, then re-prove on gridscale.

## Prometheus half

E14-S03's ScrapeConfig (`<ip>:2019/metrics`, `job="caddy"`, GSK `monitoring` ns) + `up=1` proof needs a
**serving** Nix target, so it is blocked on the boot contract above. The pattern is identical to the live
Caddy `job="caddy"` contract (E13-S05) — only the Nix VM as the target is pending.

## Cost discipline

Both attempts + all intermediate resources were torn down immediately (`server/storage/ipv4` each HTTP
204; orphan check 0/0/0; tenant back to the standing 3 GSK servers). Two ephemeral VMs ≈ €0.10 total.
The `kaddy-nix` Marketplace app + `.gz` persist (the durable E14-S02 deliverable).

## Honest bottom line

E14 is **2/3 live-proven** (S01 build + S02 register) with the deploy *mechanism* proven; the from-scratch
NixOS **boot-to-serve on gridscale** (E14-S03, ADR-0303's crisp pass/fail) is **not yet green** and needs
one dedicated iteration with console access — most likely the `.gz` snapshot-format fix (hypothesis 3).
