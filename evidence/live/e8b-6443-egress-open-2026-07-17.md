# Live proof — GSK API `:6443` egress OPEN (VPN disconnected) — 2026-07-17

**Headline:** The corporate-egress block that made the GSK API `:6443` unreachable
last session (loop2 close: "E8b app-layer ⛔ blocked — GSK API :6443 unreachable —
corporate egress allowlist") is **GONE** once the operator disconnected the
corporate VPN. E8b app-layer is now **environment-unblocked**.

## Cluster (ephemeral, cost-disciplined — provisioned for this test only)

- Stack: `stacks/gridscale/k8s` (`gridscale_k8s.platform`), release `1.30`, ONE node pool
  (1 node / 2c / 4 GiB / 30 GiB `storage_insane`) — minimal by design.
- `cluster_uuid = b9703c9b-6270-4d95-90b0-f9675a047110`
- API endpoint: `https://185.241.34.52:6443`
- Provisioned with **local state** (generated S3 backend temporarily moved aside for
  the throwaway test; restored after teardown — no repo state change).

## The `:6443` test (the operator's headline question)

```console
$ nc -zvw8 185.241.34.52 6443
Connection to 185.241.34.52 port 6443 [tcp/sun-sr-https] succeeded!

$ KUBECONFIG=<gsk> kubectl get nodes --request-timeout=20s
NAME            STATUS   ROLES    AGE    VERSION
node-pool-0-0   Ready    <none>   118s   v1.30.14
```

Both the raw TCP connect AND a real Kubernetes API round-trip (`get nodes`) succeed —
last session both timed out from the allowlisted corporate network. **VPN off → open.**

## What this un-gates

- The D-039/loop2 "E8b app-layer environment-blocked (not a defect)" note is resolved
  at the network layer. The remaining app-layer work is a genuine **integration task**,
  not an environment block: `bootstrap:argocd`/`bootstrap:e3` hard-guard to
  `kind-kaddy-dev` (must be applied manually against the GSK kubeconfig), and the
  app-of-apps assumes Cilium + Gateway API whereas GSK ships its own CNI — so the
  Cilium `NetworkPolicy`/`CiliumNetworkPolicy` + Gateway API `HTTPRoute` surfaces need
  GSK-CNI adaptation before a full sync. (See the timeboxed attempt below / follow-up.)

## Teardown

Ruthless per cost discipline — `tofu destroy` immediately after the test:
`gridscale_k8s.platform: Destruction complete after 4m49s` → `Destroy complete! Resources: 1 destroyed.`
Cluster lifetime ~7 min (created 20:44 → destroyed 20:51). Post-teardown gridscale API
audit: **0 paas services / 0 servers / 0 object-storage accesskeys** — tenant clean.
Generated S3 backend file restored; `stacks/gridscale/k8s` working tree clean vs git.
