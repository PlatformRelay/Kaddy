# promtool test rules — marshal alerting (E5)
# Placeholder until deploy/monitoring/rules/ lands (E5-S03). Wire real rule_files then.
# Run: promtool test rules tests/promtool/marshal.test.yaml
#
# rule_files:
#   - /tmp/marshal-caddy.rules.yaml
# evaluation_interval: 1m
# tests:
#   - interval: 1m
#     input_series:
#       - series: 'up{job="caddy"}'
#         values: '1 1 0 0 0 0'
#     alert_rule_test:
#       - eval_time: 5m
#         alertname: CaddyTargetDown
#         exp_alerts:
#           - exp_labels:
#               severity: critical
#               service: caddy
#
# Empty test set is valid and passes until rules exist.
tests: []
