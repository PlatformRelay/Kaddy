# REQ-E10-S04-03: the ArgoCD community plugin shows sync + health + deploy
# history for the site's Application on the entity page.
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: portal-argocd-status-card
  annotations:
    kaddy.io/req: REQ-E10-S04-03
    # skip: true — needs a running Backstage with the argocd plugin + a
    # read-only ArgoCD account. Live-cycle (deferred).
    kaddy.io/status: live-cluster-only-backstage-readpath-deferred
spec:
  skip: true
  concurrent: false
  description: ArgoCD sync/health/history rendered on the site entity page
  steps:
    - name: entity-returns-argocd-status
      try:
        - script:
            timeout: 120s
            content: |
              set -euo pipefail
              # The portal entity API returns ArgoCD sync/health for the site
              # Application (read-only ArgoCD account).
              echo "argocd status card render — live read-path check (deferred)"
              exit 0
