apiVersion: v1
kind: Pod
metadata:
  name: unlabeled
  namespace: default
  labels:
    app.kubernetes.io/name: bad-example
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
